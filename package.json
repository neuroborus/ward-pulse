{
  "name": "ward-pulse",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "docs/site"
  ],
  "scripts": {
    "design:export": "node tools/openpencil.mjs export",
    "docs:dev": "npm run dev --workspace @wardpulse/docs",
    "docs:build": "npm run build --workspace @wardpulse/docs",
    "docs:preview": "npm run preview --workspace @wardpulse/docs"
  },
  "engines": {
    "node": "^24.0.0"
  },
  "packageManager": "npm@11.16.0",
  "devDependencies": {
    "@open-pencil/cli": "0.13.2"
  },
  "overrides": {
    "@open-pencil/core": {
      "expr-eval": "npm:expr-eval-fork@3.0.3"
    }
  }
}
