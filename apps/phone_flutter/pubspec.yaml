name: ward_pulse_phone
description: WardPulse phone dashboard shell.
publish_to: "none"

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  ward_pulse_bindings:
    path: ../../bindings/dart

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
