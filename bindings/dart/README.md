# Dart Bindings

Generated Dart bindings and hand-written Flutter wrappers for the Rust core.

- `generated/` is reproducible output.
- `wrappers/` contains stable Flutter-facing APIs.

The source-of-truth interface belongs in `core/ward-pulse-ffi/`.
