# Kotlin Bindings

Generated Kotlin bindings and hand-written Android wrappers for the Rust core.

- `generated/` is reproducible output.
- `wrappers/` contains Android-facing APIs.

The source-of-truth interface belongs in `core/ward-pulse-ffi/`.
