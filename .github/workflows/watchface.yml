name: Watch Face

on:
  pull_request:
    branches: [main]
    paths:
      - "apps/watchface_wff/**"
      - ".github/workflows/watchface.yml"

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: enable WFF build and validation during Phase 6."
