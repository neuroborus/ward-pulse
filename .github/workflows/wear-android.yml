name: Wear Android

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: enable Wear OS Gradle test and assembleDebug during Phase 4."
